[pytest]
# Only the pure-Python converter suite under tests/ runs under pytest.
# test_ws.py and verify_mcp.py are live smoke scripts that need a running
# MuseScore on port 8765 and are meant to be run directly, not collected here.
testpaths = tests
